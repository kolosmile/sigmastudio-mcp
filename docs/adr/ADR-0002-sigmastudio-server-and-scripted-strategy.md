# ADR-0002: SigmaStudioServer + IScripted strategy

Status: accepted

A Bridge late-bound SigmaStudioServer adaptert használ. Generált script csak verified, escaped runtime contract után kapcsolható be; felhasználói string közvetlen script-interpolációja tilos.
